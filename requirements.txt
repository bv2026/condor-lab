# Iron Condor Backtesting Requirements
# Install with: pip install -r requirements.txt

yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
requests>=2.28.0
openpyxl>=3.0.0  # Optional: Excel export (falls back to CSV without it)

